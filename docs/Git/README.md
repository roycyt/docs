# Git

## Alias

```
git config --global alias.b 'branch -vv' 
git config --global alias.co 'checkout' 

```

```
[alias]
b = branch -vv
c = commit
ca = commit --amend --reset-author
co = checkout
f = fetch --all -p -v
l = log
s = status -s
ss = status -s -uno
```
